{
  "name": "warranted",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run --project unit",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "hono": "^4.12.12",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
